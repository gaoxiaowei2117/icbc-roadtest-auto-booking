# Windows 操作手册(零基础版)

这份手册写给没有编程基础的 Windows 用户。**全程跟着做,大约 10-15 分钟就能上手**。

## 你将得到什么

一个本地运行的网页面板 — 在浏览器里填好你的 ICBC 信息,点一下就能 24 小时自动盯 ICBC 路考的可预约时间,有合适的就自动下单。不需要你一直盯着屏幕。

## 准备工作(只做一次)

### 第 1 步:装 Python

> Python 是这个程序的「发动机」。免费、安全、约 30 MB。

1. 打开浏览器,访问 <https://www.python.org/downloads/>
2. 点页面上**大大的黄色按钮**「Download Python 3.x.x」(版本号能下到几就下到几,只要不低于 3.7 即可)
3. 双击下载好的 `python-3.x.x-amd64.exe`
4. **【最重要的一步,不勾就要白装】** 安装界面**最底下**有一行勾选项,**勾选**:
   ```
   ☑ Add Python 3.x to PATH
   ```
5. 然后点「Install Now」
6. 等 1-2 分钟,看到「Setup was successful」就点 Close

> 💡 如果 Windows 弹「Microsoft Defender SmartScreen 阻止」之类的提示,点「更多信息」→「仍要运行」。python.org 是官方网站,可以放心。

### 第 2 步:下载本项目

1. 浏览器打开 <https://github.com/gaoxiaowei2117/icbc-roadtest-auto-booking>
2. 点页面右上角绿色的「**< > Code**」按钮 → 弹出菜单里点「**Download ZIP**」
3. 浏览器下载到的应该是 `icbc-roadtest-auto-booking-main.zip`
4. **解压到一个路径全英文、没有空格的位置**,例如:
   ```
   C:\icbc-roadtest
   ```
   > ⚠️ **不要**放在 `桌面`、`我的文档` 之类有中文的路径,会导致路径乱码报错。

### 第 3 步:打开命令行,进入项目文件夹

「命令行」就是那个黑色窗口,你只需要复制粘贴就够了,不可怕。

1. 按键盘 `Win + R`(Win 键就是 Ctrl 旁边那个 Windows 图标的键)
2. 弹出小窗口,输入:
   ```
   cmd
   ```
   然后按回车
3. 黑窗口出来了。复制下面这行,**改成你实际解压的路径**,粘贴到黑窗口,回车:
   ```
   cd C:\icbc-roadtest
   ```
   > 💡 在 cmd 里粘贴可以用右键。如果右键不行,按 `Ctrl + V`。
4. **验证 Python 装好了**,输入:
   ```
   python --version
   ```
   应该看到类似 `Python 3.12.5`。
   - 如果看到「**'python' 不是内部或外部命令**」→ 回到第 1 步,**多半是没勾「Add to PATH」**,需要把 Python 卸了重装并勾上那一项。

### 第 4 步:安装运行依赖(必做)

> 这一步把程序运行需要的「零件」一次性装好,只做一次。

在黑窗口里(确保已经 `cd` 到项目目录),输入:

```
pip install -r requirements.txt
```

按回车,会看到一堆下载和安装的输出,大约 1-3 分钟。看到底部出现 `Successfully installed ...` 就好了。

> ⚠️ 如果下载特别慢或失败(国内常见),换国内镜像源:
> ```
> pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 第 5 步:申请一个 Gmail 应用专用密码(必做)

> 程序需要从你的 Gmail 邮箱**自动读 ICBC 发来的验证码**,所以要给它一个**专用密码**(不是你登录用的密码)。这个密码只能用来收发邮件,泄露了也用不来登录,比直接给主密码安全得多。

1. 用浏览器登录 <https://myaccount.google.com/>
2. 左侧菜单点「**安全性**」(Security)
3. 找到「**两步验证**」(2-Step Verification)
   - 如果是**关闭**状态:先点开,按提示开启(需要绑定手机号收验证短信)
4. 两步验证开启后,在同一页面**往下滚**,找到「**应用专用密码**」(App passwords)
   - 如果找不到,在 Google 账户页搜索栏搜「app password」就能直接进入
5. 点进去,可能要求你**再输一次主密码**
6. 在「**应用名称**」框里随便填,比如 `ICBC 抢号`,然后点「**创建**」
7. 屏幕会出现一个**像 `abcd efgh ijkl mnop` 的 16 位密码**(4 组各 4 个字符,中间有空格)
8. **立刻复制保存**——这个密码只显示一次,关掉页面就再也看不到了。粘贴到记事本里暂存。

> 💡 这个密码后面要填到面板的「Gmail 应用密码」字段里。

## 日常使用

### 启动控制面板

每次想用,回到黑窗口(在项目目录下),输入:

```
python webui.py
```

按回车后:
1. 黑窗口会打印 `http://localhost:8787`
2. 你的默认浏览器**自动**打开这个地址,看到「🚗 ICBC 自动预约控制面板」

> 💡 如果浏览器没自动开,手动复制黑窗口里那个 `http://localhost:8787` 地址,粘到浏览器地址栏回车即可。

### 配置(只做一次)

浏览器里:

1. 点顶上「**配置**」标签
2. 填好每一项:
   - **ICBC 账户**:姓氏(Last name)、驾照号(8 位数字)、ICBC 关键字/密码(给 ICBC 设置的那个,不是 Gmail 的)
   - **日期 / 时间**:用日期选择器选可接受的最早/最晚日期;用时间选择器选可接受的时间段;勾选可接受的星期几和上午/下午
   - **Gmail**:勾选「启用 Gmail」,填你的 Gmail 邮箱地址,「Gmail 应用密码」粘贴第 5 步保存的 16 位密码
   - **自动预约**:勾选「启用自动预约」,其他保持默认即可
3. 点最底下的「**保存配置**」,看到「**已保存 N 项**」就 OK 了
4. 如果页面顶上还显示「⚠️ 还需填写:...」,说明那几项没填或还是占位符值,补一下再保存

### 启动监控

回到「**监控**」标签,点蓝色的「**启动监控**」按钮。

- 顶栏的指示灯从灰色「● 已停止」变成绿色「● 监控中」
- 下面的「**实时输出**」区会开始滚动显示 `road.py` 的工作日志(每 10-15 秒一行)
- 程序会持续盯着 ICBC,**只要有合适的时间就自动下单**,然后自己退出

### 关闭程序

**正确的关闭方式**:回到那个黑窗口,按 `Ctrl + C`。会看到:
```
⏹  正在停止监控...
✅ 已退出
```

> ⚠️ **只关浏览器标签页**:监控不会停,因为服务还在后台跑。要彻底停,必须回黑窗口 Ctrl + C(或者关掉那个黑窗口)。

## 出错了怎么办

### `'python' / 'pip' 不是内部或外部命令`

**原因**:第 1 步装 Python 时**没有勾选「Add Python to PATH」**。

**解决**:
1. 控制面板 → 程序和功能 → 卸载 Python
2. 重新从 python.org 下载安装
3. 这次**一定要勾**安装界面底部的 `Add Python to PATH`

### `pip install` 卡住不动 / 报超时

**原因**:网络访问 PyPI 慢。

**解决**:用国内镜像源:
```
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 浏览器没自动打开

**解决**:看黑窗口打印的 `http://localhost:8787` 地址,手动复制到浏览器地址栏。

> 端口可能不是 8787(8787 被占用时会自动用 8788、8789...),以黑窗口实际打印的为准。

### 启动监控后实时输出框一直是「—」

可能要等几秒。如果超过 30 秒还是空的:
- 切到黑窗口看看有没有 `❌ 缺少 Python 依赖` 的红字 → 回到第 4 步重新装依赖
- 配置标签里检查 Gmail 应用密码是不是粘对了(16 位,可以有 3 个空格分组)

### 关闭面板时,Ctrl + C 没反应

少数 Windows 版本上 cmd 的 Ctrl + C 行为略不同。直接**关掉那个黑窗口**也行,效果一样。

### 黑窗口里中文显示成乱码

很少见。在程序启动前,先运行一行:
```
chcp 65001
```
再 `python webui.py`。

### 一切看起来正常,但就是抢不到号

不是程序的问题。**ICBC 系统本身就没有合适的可约时间**,程序在持续等待。把日期范围放宽、时间范围放宽,可能就能找到名额。

## 平时维护

- **配置改了**:直接在网页里改,点保存即可,**不用重启**。
- **想看历史日志**:项目目录下有个 `log/` 文件夹,`log_icbc_roadtest_checker.log` 就是详细日志。
- **更新到新版**:重新下载 ZIP,把里面的 `*.py`、`webui/`、`requirements.txt` 覆盖到现有目录(**不要覆盖 `config.yml`**,那是你的配置)。如果 `requirements.txt` 变了,重跑一次 `pip install -r requirements.txt`。

## 安全提醒

- **永远不要**把 `config.yml` 发给别人,里面有你的驾照号和 Gmail 应用密码。
- **不要**截图把 `config.yml` 或控制面板的「配置」标签页发出去。
- Gmail 应用密码万一泄露,去 Google 账户 → 安全性 → 应用专用密码,把对应那条**撤销**即可,不影响主密码。
- 这个面板**只**绑定在你自己电脑上(`127.0.0.1`),局域网其他设备访问不到,可以放心。

---

需要更多技术细节(命令行模式、跨平台说明、自动预约工作原理),请看 [`README.zh.md`](README.zh.md) 和 [`AUTO_BOOKING_GUIDE.md`](AUTO_BOOKING_GUIDE.md)。
